### 模式匹配与引用

```rust
let b: &u32 = &10; // 假设 b 是一个引用
let &c = b;         // 左边的 & 意思是：“把 b 的壳剥掉，里面的值给 c”
```

### 迭代器的隐式借用

在 Rust 中，对集合的引用进行 for 循环，迭代器默认产出的是集合元素的引用。

### Rust强制类型转换

Rust 的类型转换主要分为三大类：as 关键字（强转）、From/Into 特征（安全转换），以及隐式类型强制转换（Coercion）。

